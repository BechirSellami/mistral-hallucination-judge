# Requirements — pinned to the training environment (Colab, L4 GPU, CUDA 12.8).
# This is the source of truth for the reported results.
#
# torch carries its CUDA build (+cu128) explicitly — a bare `torch==2.11.0`
# would resolve to a CPU or mismatched-CUDA wheel. Install torch from the
# PyTorch cu128 index:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
#
# Data prep and training both used datasets 5.0.0. USB's script-based loader may
# require `datasets<3.0` on some setups — see README.

--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.11.0+cu128
transformers==5.12.1
peft==0.19.1
trl==1.7.0
bitsandbytes==0.49.2
accelerate==1.14.0
datasets==5.0.0
wandb==0.28.0
